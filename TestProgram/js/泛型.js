"use strict";
//  泛型
// use 类型推断
function identity(arg) {
    return arg;
}
let myIdentity = identity;
function identity1(arg) {
    return arg;
}
let myIdentity1 = identity1;
// 泛型类
class GenericNumber {
}
